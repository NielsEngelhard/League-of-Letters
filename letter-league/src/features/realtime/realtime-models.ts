export interface JoinGameRealtimeModel {
    gameId: string;
    username: string;
    userId: string;
}

export interface LeaveGameRealtimeModel {
    gameId: string;
    username: string;
    userId: string;
}